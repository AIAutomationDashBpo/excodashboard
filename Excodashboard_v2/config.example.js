/**
 * Dashboard Configuration — Template
 *
 * 1. Copy this file to config.js  (same folder)
 * 2. Fill in your Brainbase API key
 * 3. config.js is gitignored — never commit it
 */
const CONFIG = {
  API_KEY:  'your_brainbase_api_key_here',
  BASE_URL: 'https://brainbase-monorepo-api.onrender.com',
  DEBUG:    false,   // set true to log raw API payloads to the console
};
